[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zeekpy"
version = "0.1.0"
dependencies = [
    "websockets>=15.0",
]

[tool.setuptools]
py-modules = ["zeekpy"]
